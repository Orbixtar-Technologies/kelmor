# Copy to install.yaml next to install.sh, then:
#   sudo ./install.sh --non-interactive
hostname: panel.example.net
admin_email: ops@example.net
# admin_password: 'choose-at-least-12-characters'
channel: stable
acme: letsencrypt
non_interactive: true
